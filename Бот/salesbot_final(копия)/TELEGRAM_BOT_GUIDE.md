# Salesbot Telegram Bot - Полное руководство

## Обзор

Telegram бот для тренировки продаж с AI-клиентами. Использует **aiogram 3.x** для обработки сообщений и **FastAPI** для бэкенда с модулями тренировок.

## Архитектура

```
┌─────────────────────────────────────────────────────────┐
│                    TELEGRAM BOT                         │
│                   (telegram_bot.py)                     │
│                     aiogram 3.x                         │
└────────────┬────────────────────────────────────────────┘
             │
             ├─── telegram_main_menu.py
             │    └── /start, /menu, /products, /script, /stats
             │
             ├─── telegram_message_router.py
             │    └── Routes messages to active training sessions
             │
             └─── telegram/autoload.py
                  └── Auto-discovers and registers module handlers
                       │
                       ├─── modules.master_path (v3)
                       │    └── /master_path, /mp_next, /mp_reset, /mp_status
                       │
                       ├─── modules.arena (v4)
                       │    └── /arena, /arena_reset, /arena_status
                       │
                       ├─── modules.objections (v3)
                       │    └── /objections, /obj_reset, /obj_status
                       │
                       ├─── modules.upsell (v3)
                       │    └── /upsell, /upsell_reset, /upsell_status
                       │
                       └─── modules.deepseek_persona (v1)
                            └── /coach, /stylize, /persona_info

┌─────────────────────────────────────────────────────────┐
│                    FASTAPI BACKEND                      │
│                     (startup.py)                        │
│                       port 8080                         │
└────────────┬────────────────────────────────────────────┘
             │
             └─── router_autoload.py
                  └── Auto-includes all module routes
                       │
                       ├─── modules.master_path.v3.routes
                       ├─── modules.arena.v4.routes
                       ├─── modules.objections.v3.routes
                       ├─── modules.upsell.v3.routes
                       ├─── modules.trainer_* (legacy modules)
                       └─── integrations.telegram_push.v1.routes
```

## Запуск

### Windows

```bash
# Запустить все компоненты
start_core_api.bat
```

Это запустит:
1. FastAPI сервер на порту 8080
2. Telegram бот с aiogram 3.x

### Linux/Mac

```bash
# Установить зависимости
pip install -r requirements.txt

# Запустить FastAPI сервер (в одном терминале)
python -m uvicorn startup:app --host 0.0.0.0 --port 8080

# Запустить Telegram бот (в другом терминале)
python telegram_bot.py
```

## Переменные окружения

Обязательные:
- `TELEGRAM_TOKEN` или `TELEGRAM_BOT_TOKEN` - токен Telegram бота
- `DEEPSEEK_API_KEY` - ключ API DeepSeek для AI
- `DEEPSEEK_MODEL` - модель DeepSeek (по умолчанию: deepseek-chat)

Опциональные:
- `ADMIN_CHAT_ID` - ID чата админа для уведомлений
- `VOICE_API_KEY` - ключ для голосовых функций
- `TELEGRAM_PUSH_MOCK_MODE` - режим мок для пуш-уведомлений

## Доступные команды

### Главное меню
- `/start` или `/menu` - Показать главное меню со всеми модулями
- `/help` - Помощь и список команд
- `/products` - Каталог продуктов бренда "На Счастье"
- `/script` - Скрипт продаж
- `/stats` - Статистика по всем модулям тренировок

### Модули тренировок

#### 🎯 Путь Мастера (Master Path)
Полный цикл продажи от приветствия до закрытия сделки.

- `/master_path` - Начать тренировку Путь Мастера
- `/mp_next` - Перейти к следующему этапу
- `/mp_reset` - Начать заново
- `/mp_status` - Посмотреть текущий прогресс

**Этапы:**
1. Приветствие (greeting)
2. Квалификация клиента (qualification)
3. Поддержка/эмпатия (support)
4. Предложение (offer)
5. Демо (demo)
6. Закрытие сделки (final)

#### ⚔️ Арена (Arena)
Практика с AI-клиентами разных типов и эмоций.

- `/arena` - Начать тренировку в Арене
- `/arena_reset` - Новый клиент
- `/arena_status` - Посмотреть статистику

**Особенности:**
- 20 типов клиентов (молчаливый, разговорчивый, грубый, вежливый и т.д.)
- 5 эмоций (спокоен, нейтрален, раздражен, зол, взволнован)
- 3 уровня сложности

#### 🛡️ Возражения (Objections)
Тренировка работы с возражениями клиентов.

- `/objections` - Начать тренировку по возражениям
- `/obj_reset` - Новое возражение
- `/obj_status` - Посмотреть статистику

**Типы возражений:**
- Цена, недоверие, спешка, подумать, спросить супруга
- Страх обмана, слишком дорого, не нужно, позже, конкурент

**Персоны клиента:**
- Холодный, спокойный, агрессивный, с юмором

#### 🏆 Допродажи (Upsell)
Тренировка навыков допродаж и апгрейда услуг.

- `/upsell` - Начать тренировку допродаж
- `/upsell_reset` - Новый сценарий
- `/upsell_status` - Посмотреть статистику

**Пакеты услуг:**
- Basic - Песня + обработка
- Premium - Песня + видео открытка
- Gold - Песня + премиум история + видео

### Помощь коуча

#### 🎓 Коуч (/coach)
Получить совет от AI-коуча по любому вопросу продаж.

```
/coach Как ответить на возражение о цене?
/coach Что делать если клиент молчит?
```

#### ✨ Стилизация (/stylize)
Стилизовать текст под стиль бренда "На Счастье".

```
/stylize Здравствуйте, я могу помочь
```

#### 📖 Информация о персоне (/persona_info)
Показать правила общения и стиль бренда.

## Как работает тренировка

1. **Выбор модуля** - Отправь команду модуля (например, `/master_path`)
2. **Активация сессии** - Бот активирует тренировочную сессию для тебя
3. **Диалог** - Отправляй свои реплики как обычные сообщения (без команд)
4. **Обратная связь** - Получай ответы AI-клиента и советы коуча
5. **Завершение** - Используй команды reset для новой сессии

### Пример потока:

```
Ты: /master_path
Бот: 🎯 Путь Мастера - Полный цикл продажи
     📍 Текущий этап: Приветствие
     💬 Напиши свой вариант реплики...

Ты: Добрый день! Меня зовут Анна, мы создаем персональные песни...
Бот: 📍 Этап: Приветствие
     ⭐ Оценка: 85 балл(а)
     🎓 Совет коуча: Отлично! Добавь больше эмоций...

Ты: /mp_next
Бот: ✅ Переход на этап: Квалификация

Ты: Кому вы хотите подарить песню?
Бот: (оценка и совет)
```

## Разработка

### Добавление нового модуля

1. Создай структуру модуля:
```
modules/
  my_module/
    __init__.py         # Экспортирует register_telegram
    v1/
      __init__.py       # Определяет register_telegram(dp, registry)
      engine.py         # Логика модуля (MyEngine с handle, reset, snapshot)
      routes.py         # FastAPI routes (если нужны)
```

2. В `modules/my_module/__init__.py`:
```python
try:
    from .v1 import register_telegram
    __all__ = ['register_telegram']
except ImportError:
    pass
```

3. В `modules/my_module/v1/__init__.py`:
```python
from aiogram import types
from aiogram.filters import Command
from aiogram import Dispatcher

def register_telegram(dp, registry):
    @dp.message(Command("my_module"))
    async def _cmd_my_module(message: types.Message):
        # Логика команды
        await message.reply("Hello from my module!")
```

4. Модуль будет автоматически обнаружен и зарегистрирован!

### Структура Engine

Каждый модуль должен иметь Engine класс с методами:
- `__init__(user_id: str)` - Инициализация с ID пользователя
- `handle(text: str) -> dict` - Обработка входящего сообщения
- `reset()` - Сброс состояния
- `snapshot() -> dict` - Получение текущего состояния

### Миграция с aiogram 2.x на 3.x

Основные изменения:
- `@dp.message_handler(commands=["cmd"])` → `@dp.message(Command("cmd"))`
- `@dp.message_handler(lambda msg: ...)` → `@dp.message(F.text & condition)`
- `message.get_args()` → используй `CommandObject` и `command.args`
- `executor.start_polling(dp)` → `asyncio.run(dp.start_polling(bot))`

## Решение проблем

### Бот не отвечает
- Проверь что TELEGRAM_TOKEN установлен правильно
- Проверь что FastAPI сервер запущен на порту 8080
- Проверь логи в терминале

### Модуль не загружается
- Проверь что `modules/module_name/__init__.py` существует
- Проверь что `register_telegram` функция определена
- Проверь логи при запуске бота

### Конфликт портов
- Убедись что порт 8080 свободен
- Проверь что main.py не запущен одновременно с uvicorn

## Дополнительная информация

- [TELEGRAM_INTEGRATION_SUMMARY.md](TELEGRAM_INTEGRATION_SUMMARY.md) - Подробное описание интеграции
- [TELEGRAM_MODULES_RU.md](TELEGRAM_MODULES_RU.md) - Описание всех модулей
- [CONSOLIDATED_STARTUP.md](CONSOLIDATED_STARTUP.md) - Руководство по запуску

## Лицензия

© 2024 Salesbot Team. Все права защищены.
