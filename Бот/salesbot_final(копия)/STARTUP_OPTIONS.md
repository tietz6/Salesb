# Варианты запуска Salesbot

## Обзор

Salesbot можно запустить несколькими способами в зависимости от ваших потребностей.

## 🚀 Рекомендуемый способ: Полный запуск

### Windows
```cmd
start_core_api.bat
```

Запускает:
- ✅ FastAPI Backend Server (порт 8080)
- ✅ Telegram Bot с aiogram 3.x (полный функционал)

**Когда использовать:**
- Для продакшн использования
- Когда нужен полный функционал с интерактивным меню
- Для работы со всеми модулями тренировки

### Linux/Mac
```bash
# Терминал 1: Backend
python -m uvicorn startup:app --host 0.0.0.0 --port 8080

# Терминал 2: Telegram Bot
export TELEGRAM_BOT_TOKEN="ваш_токен"
export DEEPSEEK_API_KEY="ваш_ключ"
python telegram_bot.py
```

## 📦 Альтернатива: Simple Telegram Bot

### Windows
```cmd
# Сначала запустите backend вручную:
python -m uvicorn startup:app --host 0.0.0.0 --port 8080

# Затем в другом окне:
start_simple_telegram_bot.bat
```

Запускает:
- ✅ Упрощённый Telegram бот через HTTP API

**Когда использовать:**
- Для разработки и тестирования новых модулей
- Когда нужен простой API-based подход
- Для отладки взаимодействия с backend

### Linux/Mac
```bash
# Терминал 1: Backend (если ещё не запущен)
python -m uvicorn startup:app --host 0.0.0.0 --port 8080

# Терминал 2: Simple Bot
export TELEGRAM_BOT_TOKEN="ваш_токен"
export BACKEND_URL="http://127.0.0.1:8080"
python simple_telegram_bot.py
```

## ⚠️ ВАЖНО: Конфликты

### НЕ запускайте одновременно:

1. **telegram_bot.py** И **simple_telegram_bot.py** с одним токеном
   - Оба бота получают обновления от Telegram
   - Только один бот может работать с одним токеном
   - Это вызовет конфликт и неправильную работу

2. **main.py** И **uvicorn startup:app**
   - Оба пытаются использовать порт 8080
   - Используйте только uvicorn для FastAPI

## 📊 Сравнение вариантов

| Характеристика | start_core_api.bat | start_simple_telegram_bot.bat |
|----------------|-------------------|------------------------------|
| Backend сервер | ✅ Запускается автоматически | ❌ Нужно запустить вручную |
| Telegram бот | telegram_bot.py (aiogram 3.x) | simple_telegram_bot.py (HTTP API) |
| Модули тренировки | ✅ Все модули с интерактивным меню | ✅ Базовые модули через API |
| AI-Коуч | ✅ Встроенный /coach | ⚠️ Через backend API |
| Главное меню | ✅ Интерактивное с кнопками | ⚠️ Текстовые команды |
| Простота использования | ⭐⭐⭐⭐⭐ Одна команда | ⭐⭐⭐ Два шага |
| Для продакшн | ✅ Рекомендуется | ❌ Для разработки |
| Для разработки | ✅ Подходит | ✅ Подходит |

## 🔧 Только Backend (для разработки)

Если вы разрабатываете модули и хотите тестировать только API:

```bash
# Windows
python -m uvicorn startup:app --host 0.0.0.0 --port 8080 --reload

# Linux/Mac
python -m uvicorn startup:app --host 0.0.0.0 --port 8080 --reload
```

Откройте в браузере:
- http://localhost:8080/docs - Swagger документация API
- http://localhost:8080/api/public/v1/health - Проверка здоровья
- http://localhost:8080/api/public/v1/routes_summary - Список загруженных модулей

## 🧪 Проверка настройки

Перед запуском проверьте конфигурацию:

```bash
python validate_setup.py
```

Эта команда проверит:
- ✅ Переменные окружения (токены, ключи API)
- ✅ Зависимости Python
- ✅ Доступность модулей
- ✅ Регистрацию обработчиков Telegram
- ✅ FastAPI приложение

## 📝 Переменные окружения

### Обязательные

```bash
# Telegram Bot Token (от @BotFather)
TELEGRAM_BOT_TOKEN=ваш_токен
# или
TELEGRAM_TOKEN=ваш_токен
# или
TG_BOT_TOKEN=ваш_токен
```

### Рекомендуемые

```bash
# DeepSeek AI для коуча и оценок
DEEPSEEK_API_KEY=ваш_ключ_deepseek
DEEPSEEK_MODEL=deepseek-chat

# URL backend сервера (для simple_telegram_bot.py)
BACKEND_URL=http://127.0.0.1:8080

# Voice API (если используете голосовые функции)
VOICE_API_KEY=ваш_ключ_voice
```

### Опциональные

```bash
# Mock режим для тестирования без реального Telegram
TELEGRAM_PUSH_MOCK_MODE=false

# Admin chat ID для уведомлений
ADMIN_CHAT_ID=ваш_chat_id
```

## 🐛 Решение проблем

### Бот не запускается

**Проблема:** `RuntimeError: TELEGRAM_TOKEN required`

**Решение:**
```bash
# Windows
echo %TELEGRAM_BOT_TOKEN%

# Linux/Mac
echo $TELEGRAM_BOT_TOKEN
```
Если пусто, установите токен в start_core_api.bat или в переменные окружения.

### Backend недоступен

**Проблема:** `Backend сервер недоступен!`

**Решение:**
1. Проверьте, что backend запущен:
   ```bash
   curl http://localhost:8080/api/public/v1/health
   ```
2. Проверьте логи в окне SALESBOT_API
3. Убедитесь, что порт 8080 не занят другим процессом

### Конфликт ботов

**Проблема:** Оба бота запущены, но только один работает

**Решение:**
1. Остановите оба бота
2. Выберите один вариант:
   - Либо `start_core_api.bat` (telegram_bot.py)
   - Либо `start_simple_telegram_bot.bat` (simple_telegram_bot.py)
3. Запустите только выбранный вариант

### Модули не загружаются

**Проблема:** `No module named 'modules.xxx'`

**Решение:**
```bash
# Проверьте структуру:
ls modules/
ls modules/master_path/
ls modules/arena/

# Должны быть файлы __init__.py
```

## 📚 Дополнительная документация

- **QUICK_START.md** - Быстрый старт за 5 минут
- **TELEGRAM_BOT_GUIDE.md** - Полное руководство по Telegram боту
- **CONSOLIDATED_STARTUP.md** - Описание компонентов и архитектуры
- **INTEGRATION_COMPLETE.md** - История интеграции и решённые проблемы
- **TELEGRAM_MODULES_RU.md** - Описание всех модулей тренировки

## 💡 Советы

### Для пользователей
1. Используйте `start_core_api.bat` - это проще всего
2. Проверьте работу командой `/start` в Telegram
3. Изучите команды через главное меню бота

### Для разработчиков
1. Используйте `--reload` флаг для автоперезагрузки при изменениях:
   ```bash
   python -m uvicorn startup:app --host 0.0.0.0 --port 8080 --reload
   ```
2. Тестируйте новые модули через Swagger UI: http://localhost:8080/docs
3. Используйте `validate_setup.py` для проверки изменений
4. Добавьте логирование для отладки:
   ```python
   import logging
   logging.basicConfig(level=logging.DEBUG)
   ```

### Для администраторов
1. Установите кастомные токены в start_core_api.bat
2. Настройте `ADMIN_CHAT_ID` для получения уведомлений
3. Мониторьте логи в окнах SALESBOT_API и SALESBOT_TELEGRAM_BOT
4. Регулярно обновляйте зависимости:
   ```bash
   pip install -r requirements.txt --upgrade
   ```

## 🎯 Рекомендации

### Продакшн
```
✅ Используйте: start_core_api.bat
✅ Один процесс на компонент
✅ Мониторинг логов
✅ Регулярные бэкапы данных
```

### Разработка
```
✅ Используйте: uvicorn с --reload
✅ Тестируйте через Swagger UI
✅ Используйте validate_setup.py
✅ Логирование на уровне DEBUG
```

### Тестирование
```
✅ Используйте: простой подход с simple_telegram_bot.py
✅ Тестируйте каждый модуль отдельно
✅ Проверяйте API endpoints через curl/Postman
✅ Используйте TELEGRAM_PUSH_MOCK_MODE=true
```

---

**Готово к работе!** 🚀

Выберите подходящий вариант запуска и начните использовать Salesbot!
