# Архитектура Telegram бота - Salesbot

## 🏗️ Новая архитектура (Webhook mode)

### Обзор

Telegram бот теперь **полностью интегрирован в FastAPI backend** и работает через webhook вместо polling.

```
Telegram API
     ↓ (отправляет обновления через webhook)
FastAPI Backend (порт 8080)
     ↓ (обрабатывает через)
aiogram Dispatcher
     ↓ (маршрутизирует к)
Обработчики команд и сообщений
     ↓ (используют)
Модули тренировки (Master Path, Arena, Objections, Upsell)
     ↓ (отвечают через)
Telegram API (отправка сообщений пользователю)
```

### Компоненты системы

#### 1. FastAPI Backend (`startup.py`)
- **Порт**: 8080
- **Функции**:
  - Инициализация telegram бота при запуске
  - Обработка webhook-запросов от Telegram
  - Регистрация всех обработчиков команд
  - Интеграция с модулями тренировки

#### 2. Telegram Bot Dispatcher (`telegram_bot.py` логика)
- **Библиотека**: aiogram 3.x
- **Функции**:
  - Регистрация обработчиков команд
  - Маршрутизация сообщений к активным сессиям
  - Управление состоянием пользователей

#### 3. Главное меню (`telegram_main_menu.py`)
- **Команды**:
  - `/start`, `/menu`, `/help` - главное меню
  - `/products` - каталог продуктов
  - `/script` - скрипт продаж
  - `/stats` - статистика тренировок

#### 4. Роутер сообщений (`telegram_message_router.py`)
- **Функции**:
  - Отслеживание активных сессий пользователей
  - Маршрутизация текстовых сообщений к нужному модулю
  - Обработка сообщений для каждого типа тренировки

#### 5. Модули тренировки (`modules/`)
- **Master Path** (`modules/master_path/`) - полный цикл продажи
- **Arena** (`modules/arena/`) - тренировка с AI-клиентами
- **Objections** (`modules/objections/`) - работа с возражениями
- **Upsell** (`modules/upsell/`) - допродажи
- **DeepSeek Persona** (`modules/deepseek_persona/`) - AI-коуч

### Поток данных

#### Получение сообщения от пользователя:

1. Пользователь отправляет сообщение в Telegram
2. Telegram отправляет webhook-запрос на `https://your-domain.com/api/telegram/webhook`
3. FastAPI получает запрос и передает его в `telegram_webhook()`
4. Создается объект `Update` из JSON
5. Update передается в `dp.feed_update(bot, update)`
6. Dispatcher находит подходящий обработчик (handler) для команды/сообщения
7. Обработчик выполняется (например, `_cmd_start()` для команды `/start`)
8. Обработчик формирует ответ и отправляет его через `message.reply()`
9. aiogram отправляет HTTP-запрос к Telegram API для отправки сообщения
10. Пользователь получает ответ в Telegram

#### Обработка команд:

```python
# Пользователь отправляет: /start
↓
@dp.message(Command("start"))
async def _cmd_start(message: types.Message):
    await message.reply("Привет! 👋")
↓
# Telegram получает сообщение и отправляет его пользователю
```

#### Обработка текстовых сообщений в активной сессии:

```python
# Пользователь запустил /master_path
# Система установила активную сессию: USER_ACTIVE_SESSIONS[user_id] = {'type': 'master_path'}

# Пользователь отправляет текст: "Добрый день!"
↓
@dp.message(F.text & ~F.text.startswith('/'))
async def _route_message_to_active_session(message: types.Message):
    # Проверяем активную сессию
    if active_session['type'] == 'master_path':
        await _handle_master_path_message(message, user_id)
↓
# Обрабатываем через MasterPath engine
mp = MasterPath(user_id)
result = mp.handle(message.text)
↓
# Отправляем ответ с оценкой и советом коуча
await message.reply(response)
```

### Регистрация обработчиков

Каждый модуль экспортирует функцию `register_telegram(dp, registry)`:

```python
# modules/master_path/v3/__init__.py
def register_telegram(dp, registry):
    @dp.message(Command("master_path"))
    async def _cmd_master_path(message: types.Message):
        # ... обработка команды
```

Автозагрузчик (`telegram/autoload.py`) автоматически находит и вызывает эти функции:

```python
# Для каждого модуля в packages/modules/*
for module in modules:
    if hasattr(module, 'register_telegram'):
        module.register_telegram(dp, registry)
```

## 🔄 Сравнение с предыдущей архитектурой

### Старая архитектура (Polling mode)

```
telegram_bot.py (отдельный процесс)
     ↓ (постоянно опрашивает)
Telegram API (getUpdates)
     ↓ (получает обновления)
aiogram Dispatcher
     ↓ (обрабатывает)
Обработчики
```

**Проблемы:**
- ❌ Требуется прямой доступ к api.telegram.org
- ❌ Два отдельных процесса (FastAPI + telegram_bot.py)
- ❌ Не работает за firewall или в ограниченных сетях
- ❌ Не масштабируется (один процесс = один бот)

### Новая архитектура (Webhook mode)

```
FastAPI Backend (порт 8080)
     ↓ (получает webhook от)
Telegram API
     ↓ (обрабатывает через встроенный)
aiogram Dispatcher
     ↓ (обрабатывает)
Обработчики
```

**Преимущества:**
- ✅ Один процесс для всего (FastAPI + бот)
- ✅ Работает через reverse proxy
- ✅ Масштабируется горизонтально
- ✅ Не требует постоянного подключения
- ✅ Telegram сам отправляет обновления
- ✅ Меньше нагрузка на сеть

## 🔧 Конфигурация

### Переменные окружения

Все необходимые переменные настроены в `start_core_api.bat`:

```bash
# Telegram Bot Token (обязательно)
TELEGRAM_BOT_TOKEN=your_token_here
TELEGRAM_TOKEN=your_token_here  # альтернативное имя

# DeepSeek AI (для AI-коуча)
DEEPSEEK_API_KEY=your_key_here
DEEPSEEK_MODEL=deepseek-chat

# Voice API (для голосовых функций)
VOICE_API_KEY=your_key_here

# Backend URL (для справки)
BACKEND_URL=http://127.0.0.1:8080
```

### Webhook URL

Webhook должен быть настроен в Telegram:

```bash
# Используя наш скрипт
python setup_telegram_webhook.py https://your-domain.com

# Или вручную через Telegram API
curl -X POST "https://api.telegram.org/bot<TOKEN>/setWebhook" \
     -d "url=https://your-domain.com/api/telegram/webhook"
```

## 📊 Логирование

Все компоненты логируют свою работу в stdout:

```
[startup] Registering main menu and commands...           # Регистрация главного меню
[startup] Auto-loading module handlers...                 # Автозагрузка модулей
[tg_autoload] Registered telegram handlers for modules.X  # Каждый модуль
[startup] Telegram bot initialized successfully!          # Инициализация завершена
[telegram_webhook] Error processing update: ...           # Ошибки webhook
```

## 🧪 Тестирование

### Проверка здоровья системы

```bash
curl http://localhost:8080/api/public/v1/health
```

Ожидаемый ответ:
```json
{
  "ok": true,
  "app": "salesbot",
  "version": "v1-final",
  "telegram_bot": "enabled"
}
```

### Симуляция webhook-запроса

```bash
curl -X POST http://localhost:8080/api/telegram/webhook \
  -H "Content-Type: application/json" \
  -d '{
    "update_id": 123,
    "message": {
      "message_id": 1,
      "from": {"id": 12345, "first_name": "Test"},
      "chat": {"id": 12345, "type": "private"},
      "date": 1234567890,
      "text": "/start"
    }
  }'
```

### Проверка статуса webhook

```bash
python setup_telegram_webhook.py info
```

## 🚀 Деплой

### Локальная разработка

1. Запустите ngrok:
   ```bash
   ngrok http 8080
   ```

2. Скопируйте URL (например, `https://abc123.ngrok.io`)

3. Настройте webhook:
   ```bash
   python setup_telegram_webhook.py https://abc123.ngrok.io
   ```

4. Запустите сервер:
   ```bash
   start_core_api.bat
   ```

### Продакшн

1. Настройте домен с SSL (обязательно HTTPS!)

2. Настройте reverse proxy (nginx/caddy):
   ```nginx
   location /api/telegram/webhook {
       proxy_pass http://localhost:8080;
   }
   ```

3. Настройте webhook:
   ```bash
   python setup_telegram_webhook.py https://your-domain.com
   ```

4. Запустите с systemd/supervisor для автозапуска

## 📚 Дополнительная информация

- `TELEGRAM_WEBHOOK_SETUP.md` - Полное руководство по настройке webhook
- `TELEGRAM_COMPLETE_GUIDE.md` - Полное руководство по использованию бота
- `STARTUP_OPTIONS.md` - Варианты запуска системы
- `QUICK_START.md` - Быстрый старт за 5 минут

## ❓ FAQ

**Q: Почему webhook лучше polling?**

A: Webhook более эффективен - Telegram сам отправляет обновления, не нужно постоянно опрашивать API. Также webhook работает через proxy и масштабируется лучше.

**Q: Можно ли использовать и webhook, и polling?**

A: Нет, одновременно использовать нельзя. Нужно выбрать один режим.

**Q: Что если webhook недоступен?**

A: Telegram будет пытаться отправить обновление несколько раз. Если webhook недоступен долго, обновления могут быть потеряны. Используйте polling для разработки без публичного URL.

**Q: Как вернуться к polling?**

A: Удалите webhook (`python setup_telegram_webhook.py delete`) и используйте старый файл `telegram_bot.py`.
