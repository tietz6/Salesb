# Исправление подключения Telegram бота

## 🎯 Что было сделано

Telegram бот не подключался из-за сетевых ограничений (блокировка доступа к `api.telegram.org`). Были добавлены следующие улучшения:

### 1. ✅ Диагностический инструмент

**Файл:** `test_telegram_connection.py`

Этот скрипт проверяет:
- Наличие и правильность токена
- Доступность Telegram API
- Информацию о боте
- Диагностирует типичные проблемы

**Использование:**
```bash
python test_telegram_connection.py
```

### 2. ✅ Улучшенная обработка ошибок

**Файлы:** `telegram_bot.py`, `run_bot.py`

Добавлено:
- Проверка подключения перед запуском
- Подробные сообщения об ошибках
- Советы по решению проблем
- Корректное закрытие сессий

### 3. ✅ Поддержка пользовательского API сервера

**Функция:** Возможность использовать локальный Bot API Server или прокси

**Использование:**
```bash
# Установить переменную окружения
set TELEGRAM_API_SERVER=http://localhost:8081

# Запустить бота
python telegram_bot.py
```

### 4. ✅ Режим Webhook

**Файл:** `telegram_webhook.py`

Альтернативный режим для случаев, когда polling не работает.

**Требования:**
- Публичный HTTPS URL
- Открытый порт (443, 80, 88, или 8443)

**Использование с ngrok (для тестирования):**
```bash
# Терминал 1: Запустить ngrok
ngrok http 8443

# Терминал 2: Установить URL и запустить бота
set WEBHOOK_URL=https://abc123.ngrok.io
python telegram_webhook.py
```

### 5. ✅ Удобный стартовый скрипт

**Файл:** `start_telegram.bat`

Интерактивное меню с опциями:
1. Диагностика подключения
2. Запуск в режиме Polling
3. Запуск в режиме Webhook
4. Запуск с пользовательским API сервером
5. Показать справку

**Использование:**
```bash
start_telegram.bat
```

### 6. ✅ Полное руководство по решению проблем

**Файл:** `TELEGRAM_TROUBLESHOOTING.md`

Содержит:
- Диагностику различных проблем
- 6 различных решений
- Пошаговые инструкции
- FAQ
- Полезные ссылки

---

## 🚀 Быстрый старт

### Шаг 1: Проверка подключения

```bash
cd "Бот/salesbot_final(копия)"
python test_telegram_connection.py
```

### Шаг 2: Выбор режима запуска

#### Вариант A: Стандартный режим (если тест успешен)
```bash
python telegram_bot.py
```

#### Вариант B: Использование меню запуска
```bash
start_telegram.bat
```

#### Вариант C: Режим Webhook (если polling не работает)
```bash
# С ngrok для тестирования
ngrok http 8443

# В другом терминале
set WEBHOOK_URL=https://ваш-ngrok-url.ngrok.io
python telegram_webhook.py
```

#### Вариант D: С локальным API сервером
```bash
# Запустить Bot API Server (Docker)
docker run -d -p 8081:8081 --name telegram-bot-api aiogram/telegram-bot-api

# Установить переменную и запустить бота
set TELEGRAM_API_SERVER=http://localhost:8081
python telegram_bot.py
```

---

## 📋 Решение конкретных проблем

### Проблема: `Cannot connect to host api.telegram.org`

**Причина:** Telegram API недоступен (блокировка, firewall, нет интернета)

**Решения:**
1. Использовать прокси (см. `TELEGRAM_TROUBLESHOOTING.md`)
2. Настроить локальный Bot API Server
3. Использовать режим Webhook
4. Проверить настройки firewall/антивируса

### Проблема: `Unauthorized` или `Invalid token`

**Причина:** Неверный токен бота

**Решение:**
1. Получить новый токен от [@BotFather](https://t.me/BotFather)
2. Обновить в `start_core_api.bat`:
   ```batch
   set TELEGRAM_TOKEN=ВАШ_НОВЫЙ_ТОКЕН
   ```

### Проблема: Бот не отвечает на команды

**Причина:** Бот не запущен или не подключен

**Решение:**
1. Запустить диагностику: `python test_telegram_connection.py`
2. Проверить логи бота на ошибки
3. Убедиться, что бот запущен: `python telegram_bot.py`
4. Проверить, что токен правильный

---

## 🔧 Настройка переменных окружения

### Windows (start_core_api.bat):
```batch
set TELEGRAM_TOKEN=ваш_токен
set TELEGRAM_API_SERVER=http://localhost:8081  # опционально
set WEBHOOK_URL=https://your-domain.com  # для webhook режима
```

### Linux/Mac:
```bash
export TELEGRAM_TOKEN=ваш_токен
export TELEGRAM_API_SERVER=http://localhost:8081  # опционально
export WEBHOOK_URL=https://your-domain.com  # для webhook режима
```

---

## 📚 Дополнительные ресурсы

### Документация проекта
- `TELEGRAM_TROUBLESHOOTING.md` - Полное руководство по решению проблем
- `TELEGRAM_BOT_GUIDE.md` - Руководство по использованию бота
- `QUICK_START.md` - Быстрый старт

### Внешние ресурсы
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Aiogram документация](https://docs.aiogram.dev/)
- [Bot API Server](https://github.com/tdlib/telegram-bot-api)
- [ngrok](https://ngrok.com/) - для тестирования webhook

---

## 🧪 Тестирование

### Проверка подключения:
```bash
python test_telegram_connection.py
```

### Проверка токена вручную:
```bash
curl https://api.telegram.org/bot<ВАШ_ТОКЕН>/getMe
```

### Проверка webhook:
```bash
curl https://api.telegram.org/bot<ВАШ_ТОКЕН>/getWebhookInfo
```

---

## ⚙️ Архитектура решения

```
┌─────────────────────────────────────────────────┐
│         Режимы работы Telegram бота             │
├─────────────────────────────────────────────────┤
│                                                 │
│  1. Polling (telegram_bot.py)                   │
│     └─> Напрямую к api.telegram.org            │
│     └─> Через прокси                            │
│     └─> Через локальный Bot API Server         │
│                                                 │
│  2. Webhook (telegram_webhook.py)               │
│     └─> С ngrok (тестирование)                  │
│     └─> С публичным HTTPS (production)          │
│                                                 │
│  3. Диагностика (test_telegram_connection.py)   │
│     └─> Проверка токена и подключения          │
│                                                 │
└─────────────────────────────────────────────────┘
```

---

## 🎉 Результат

После применения этих исправлений:

✅ Бот может работать через прокси  
✅ Бот может работать с локальным API сервером  
✅ Бот может работать в режиме webhook  
✅ Легкая диагностика проблем  
✅ Понятные сообщения об ошибках  
✅ Несколько вариантов запуска  

---

## 💡 Рекомендации

### Для разработки:
- Используйте **polling режим** с диагностикой
- Если есть блокировка - используйте **ngrok + webhook**

### Для production:
- Используйте **локальный Bot API Server**
- Или **webhook** с настоящим HTTPS сертификатом
- Настройте мониторинг и автоперезапуск

### Для тестирования:
- Всегда начинайте с `python test_telegram_connection.py`
- Используйте `start_telegram.bat` для быстрого доступа к разным режимам

---

**Версия:** 1.0  
**Дата:** 2025-11-21  
**Автор:** GitHub Copilot
