# Как исправить проблемы с подключением Telegram бота

## 🎯 Проблема решена!

Telegram бот теперь работает через **polling** (без webhook). Бот автоматически удаляет любые webhook при запуске.

**✅ Просто запустите:**
```bash
start_core_api.bat
```

Это запустит бот в режиме polling с автоматической очисткой webhook.

---

## 🚀 Быстрое решение (3 шага)

### Шаг 1: Диагностика
```bash
python test_telegram_connection.py
```
Этот скрипт проверит:
- ✅ Правильность токена
- ✅ Доступ к Telegram API
- ✅ Даст рекомендации по исправлению

### Шаг 2: Выбор способа запуска

**Рекомендуется:** Запустите всю систему через:
```bash
start_core_api.bat
```
Это автоматически запустит бот в режиме polling (без webhook).

**Альтернативно:** Используйте интерактивное меню:
```bash
start_telegram.bat
```

Или запустите напрямую:
```bash
# Стандартный режим (рекомендуется)
python telegram_bot.py

# ⚠️ Режим webhook НЕ РЕКОМЕНДУЕТСЯ для обычного использования
# python telegram_webhook.py
```

### Шаг 3: Проверка
Откройте Telegram и отправьте боту команду `/start`

---

## 📋 Что делать если бот не подключается?

### ✅ Сначала попробуйте стандартный запуск:
```bash
start_core_api.bat
```
Это запустит бот в режиме polling с автоматическим удалением webhook.

### ❌ Ошибка: "Cannot connect to host api.telegram.org"

**Это означает:** Telegram API заблокирован или недоступен

**Решение 1 - Локальный API сервер (Рекомендуется):**
```bash
# В start_core_api.bat добавьте:
set TELEGRAM_API_SERVER=http://localhost:8081

# Или запустите:
start_telegram.bat
# И выберите пункт 4
```

**Решение 2 - Подробная диагностика:**
```bash
# Посмотрите полное руководство
notepad TELEGRAM_TROUBLESHOOTING.md
```

**⚠️ ВНИМАНИЕ:** Режим webhook НЕ РЕКОМЕНДУЕТСЯ для обычного использования.
Используйте только если polling не работает и вы понимаете, как настроить HTTPS.
Бот теперь автоматически удаляет любые webhook при запуске в режиме polling.

### ❌ Ошибка: "Unauthorized" или "Invalid token"

**Это означает:** Неправильный токен

**Решение:**
1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте `/token` и выберите вашего бота
4. Скопируйте новый токен
5. Вставьте в `start_core_api.bat`:
```batch
set TELEGRAM_TOKEN=ВАШ_НОВЫЙ_ТОКЕН
```

---

## 🛠️ Доступные инструменты

### 1. Диагностика подключения
```bash
python test_telegram_connection.py
```
Проверяет токен и доступность API

### 2. Интерактивное меню запуска
```bash
start_telegram.bat
```
Выбор из 5 режимов работы

### 3. Стандартный бот (polling) ✅ РЕКОМЕНДУЕТСЯ
```bash
python telegram_bot.py
```
Обычный режим long polling (автоматически удаляет webhook)

### 4. Полный запуск всей системы ✅ РЕКОМЕНДУЕТСЯ
```bash
start_core_api.bat
```
API сервер + Telegram бот вместе (режим polling, без webhook)

### 5. Бот через webhook ⚠️ НЕ РЕКОМЕНДУЕТСЯ
```bash
# python telegram_webhook.py
```
Альтернативный режим - используйте только если polling не работает

---

## 📚 Документация

Созданы новые руководства:

1. **TELEGRAM_CONNECTION_FIX.md** - Что было исправлено и как использовать
2. **TELEGRAM_TROUBLESHOOTING.md** - Полное руководство по решению проблем (6 решений)
3. **test_telegram_connection.py** - Скрипт диагностики
4. **start_telegram.bat** - Интерактивное меню запуска

---

## ✅ Проверка работы

После запуска бота:

1. Откройте Telegram
2. Найдите своего бота (в нашем случае это бот с токеном из start_core_api.bat)
3. Отправьте команду: `/start`
4. Должно прийти приветственное сообщение с меню

**Доступные команды:**
- `/start` - Главное меню
- `/master_path` - Путь Мастера
- `/arena` - Арена с AI
- `/objections` - Работа с возражениями
- `/upsell` - Допродажи
- `/products` - Каталог продуктов
- `/script` - Скрипт продаж

---

## 💡 Рекомендации

### Для обычного использования (✅ РЕКОМЕНДУЕТСЯ):
1. Запустите `start_core_api.bat` - это запустит всё в режиме polling
2. Бот автоматически удалит любые webhook при запуске
3. Если бот не отвечает - запустите диагностику: `python test_telegram_connection.py`
4. Следуйте рекомендациям из диагностики

### Если есть проблемы с сетью:
1. Используйте `start_telegram.bat` и выберите режим 4 (пользовательский API)
2. Настройте локальный Bot API Server

### ⚠️ Webhook НЕ рекомендуется:
- Webhook требует публичный HTTPS URL
- Webhook сложнее в настройке и отладке
- Polling работает лучше для большинства случаев
- Бот теперь автоматически очищает webhook при запуске

---

## 🆘 Нужна помощь?

1. Запустите диагностику: `python test_telegram_connection.py`
2. Прочитайте `TELEGRAM_TROUBLESHOOTING.md`
3. Используйте интерактивное меню: `start_telegram.bat`
4. Проверьте логи бота на ошибки

---

## ⚡ Краткая справка по файлам

| Файл | Назначение |
|------|-----------|
| `test_telegram_connection.py` | Диагностика подключения |
| `telegram_bot.py` | Основной бот (polling) |
| `telegram_webhook.py` | Бот в режиме webhook |
| `start_telegram.bat` | Интерактивное меню |
| `start_core_api.bat` | Полный запуск системы |
| `TELEGRAM_TROUBLESHOOTING.md` | Полное руководство |
| `TELEGRAM_CONNECTION_FIX.md` | Описание исправлений |

---

**Токены уже установлены в `start_core_api.bat`** ✅  
**Все зависимости установлены** ✅  
**Осталось только запустить!** 🚀

---

*Последнее обновление: 2025-11-21*
