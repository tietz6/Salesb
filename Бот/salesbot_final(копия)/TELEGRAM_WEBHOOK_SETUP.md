# Настройка Telegram Webhook для Salesbot

## 🎯 Что изменилось

Telegram бот теперь **интегрирован в FastAPI сервер** и работает через webhook вместо polling. Это означает:

- ✅ Все компоненты запускаются одной командой `start_core_api.bat`
- ✅ Бот работает через FastAPI backend на порту 8080
- ✅ Все модули тренировки загружаются автоматически
- ✅ Не нужен прямой доступ к api.telegram.org

## 🚀 Быстрый старт

### Шаг 1: Запустите систему

```bash
start_core_api.bat
```

Это запустит FastAPI сервер с интегрированным Telegram ботом на порту 8080.

### Шаг 2: Получите публичный URL

Чтобы Telegram мог отправлять обновления вашему боту, вам нужен публичный URL (доступный из интернета).

#### Вариант A: Использование ngrok (рекомендуется для тестирования)

1. Установите [ngrok](https://ngrok.com/)
2. Запустите в новом окне терминала:
   ```bash
   ngrok http 8080
   ```
3. Скопируйте URL (например, `https://abc123.ngrok.io`)

#### Вариант B: Использование localtunnel

1. Установите localtunnel:
   ```bash
   npm install -g localtunnel
   ```
2. Запустите:
   ```bash
   lt --port 8080
   ```
3. Скопируйте URL

#### Вариант C: Собственный домен

Если у вас есть собственный домен и сервер:
- Настройте reverse proxy (nginx, caddy)
- Направьте трафик на порт 8080
- Используйте HTTPS (обязательно для Telegram)

### Шаг 3: Настройте webhook

Используйте наш скрипт для настройки webhook:

```bash
python setup_telegram_webhook.py https://your-public-url.com
```

Например, если вы используете ngrok:
```bash
python setup_telegram_webhook.py https://abc123.ngrok.io
```

## 📋 Проверка статуса webhook

Чтобы проверить текущий статус webhook:

```bash
python setup_telegram_webhook.py info
```

Вы увидите:
- Текущий URL webhook
- Количество ожидающих обновлений
- Информацию об ошибках (если есть)

## 🔧 Отладка

### Проверить, что сервер работает

```bash
curl http://localhost:8080/api/public/v1/health
```

Ответ должен быть:
```json
{
  "ok": true,
  "app": "salesbot",
  "version": "v1-final",
  "telegram_bot": "enabled"
}
```

### Проверить webhook endpoint

```bash
curl -X POST http://localhost:8080/api/telegram/webhook -H "Content-Type: application/json" -d "{}"
```

### Просмотр логов

Логи отображаются в окне SALESBOT_API. Следите за сообщениями:
- `[startup] Telegram bot initialized successfully!` - бот инициализирован
- `[telegram_webhook] Error processing update:` - ошибки обработки

### Удаление webhook (переход на polling)

Если нужно вернуться к старому режиму:

```bash
python setup_telegram_webhook.py delete
```

## 🌐 Работа в продакшене

Для продакшн-окружения:

1. **Используйте HTTPS** - Telegram требует HTTPS для webhook
2. **Используйте стабильный URL** - не ngrok, который меняет URL
3. **Настройте reverse proxy** - nginx или caddy для SSL
4. **Используйте systemd/supervisor** - для автозапуска сервера

Пример конфигурации nginx:

```nginx
server {
    listen 443 ssl;
    server_name your-domain.com;

    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;

    location /api/telegram/webhook {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## ❓ FAQ

**Q: Бот не отвечает на команды**

A: Проверьте:
1. Запущен ли сервер (`start_core_api.bat`)
2. Настроен ли webhook (`python setup_telegram_webhook.py info`)
3. Доступен ли публичный URL (проверьте ngrok или другой туннель)
4. Логи в окне SALESBOT_API

**Q: Ошибка "telegram bot not initialized"**

A: Проверьте, что `TELEGRAM_BOT_TOKEN` установлен в `start_core_api.bat`

**Q: Нужно ли менять webhook при каждом запуске?**

A: Если вы используете ngrok, да - URL меняется при каждом запуске. Для продакшена используйте постоянный домен.

**Q: Можно ли использовать polling вместо webhook?**

A: Старый файл `telegram_bot.py` все еще доступен, но новая архитектура с webhook более стабильна и рекомендуется.

**Q: Как тестировать локально без публичного URL?**

A: Используйте ngrok или localtunnel - они создают временный публичный URL для вашего локального сервера.

## 📚 Дополнительные ресурсы

- [Telegram Bot API - Webhooks](https://core.telegram.org/bots/api#setwebhook)
- [ngrok Documentation](https://ngrok.com/docs)
- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [aiogram Documentation](https://docs.aiogram.dev/)

## 🎓 Доступные команды бота

После настройки webhook бот будет отвечать на следующие команды:

- `/start` или `/menu` - Главное меню
- `/master_path` - Путь Мастера (полный цикл продажи)
- `/arena` - Арена с AI-клиентами
- `/objections` - Работа с возражениями
- `/upsell` - Допродажи
- `/products` - Каталог продуктов
- `/script` - Скрипт продаж
- `/stats` - Статистика тренировок
- `/coach <вопрос>` - Получить совет от AI-коуча
- `/stylize <текст>` - Стилизация текста под бренд

Все модули тренировки работают через обычные текстовые сообщения после запуска соответствующей команды!
