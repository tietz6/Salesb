# Что изменилось в Telegram боте

## 🎯 Главное изменение

**Telegram бот теперь интегрирован в FastAPI и работает через webhook!**

## ❌ Раньше (Polling mode)

```
start_core_api.bat запускал:
├── Окно 1: FastAPI сервер (startup.py)
└── Окно 2: Telegram бот (telegram_bot.py)

Бот постоянно опрашивал api.telegram.org
❌ Требовал прямое подключение к Telegram
❌ Не работал в ограниченных сетях
❌ Два отдельных процесса
```

## ✅ Теперь (Webhook mode)

```
start_core_api.bat запускает:
└── Одно окно: FastAPI + Telegram бот

Telegram сам отправляет обновления на ваш сервер
✅ Один процесс для всего
✅ Работает через любой reverse proxy
✅ Не нужен прямой доступ к Telegram API
✅ Более надежно и масштабируемо
```

## 🚀 Что нужно сделать

### 1. Ничего не менять в токенах

Ваши токены в `start_core_api.bat` остаются такими же:
```batch
set TELEGRAM_BOT_TOKEN=8029409301:AAGpKsSxQ_rdQJm_5kR6hk_E5JgOoQLNAgI
set DEEPSEEK_API_KEY=sk-4de54b0041b44cf193b22cf21f028be7
```

### 2. Запустить систему как обычно

```cmd
start_core_api.bat
```

Теперь запустится ТОЛЬКО FastAPI с интегрированным ботом (одно окно вместо двух).

### 3. Настроить webhook (один раз)

Вам нужен публичный URL для вашего локального сервера.

#### Вариант A: Использовать ngrok (проще всего)

1. Скачайте ngrok: https://ngrok.com/download
2. Запустите в новом окне:
   ```cmd
   ngrok http 8080
   ```
3. Скопируйте URL (например: `https://abc123.ngrok.io`)
4. Настройте webhook:
   ```cmd
   python setup_telegram_webhook.py https://abc123.ngrok.io
   ```

#### Вариант B: Использовать свой домен

Если у вас есть сервер с доменом:
```cmd
python setup_telegram_webhook.py https://ваш-домен.com
```

### 4. Всё! Бот работает

Теперь отправьте `/start` вашему боту в Telegram - он должен ответить!

## 📝 Основные команды

### Проверить статус webhook
```cmd
python setup_telegram_webhook.py info
```

### Удалить webhook (вернуться к старому режиму)
```cmd
python setup_telegram_webhook.py delete
```

### Проверить, что сервер работает
```cmd
curl http://localhost:8080/api/public/v1/health
```

## 🔍 Проверка работы

### 1. Запустите систему
```cmd
start_core_api.bat
```

Должно появиться одно окно: `SALESBOT_API`

### 2. Проверьте логи

В окне должно быть:
```
[startup] Telegram bot initialized successfully!
INFO:     Uvicorn running on http://0.0.0.0:8080
```

### 3. Проверьте health endpoint

Откройте в браузере:
```
http://localhost:8080/api/public/v1/health
```

Должен показать:
```json
{
  "ok": true,
  "app": "salesbot",
  "version": "v1-final",
  "telegram_bot": "enabled"
}
```

### 4. Настройте webhook

```cmd
# Запустите ngrok в отдельном окне
ngrok http 8080

# Скопируйте URL и настройте webhook
python setup_telegram_webhook.py https://your-ngrok-url.ngrok.io
```

### 5. Проверьте статус webhook

```cmd
python setup_telegram_webhook.py info
```

Должно показать ваш webhook URL.

### 6. Тестируйте бота

Отправьте `/start` вашему боту в Telegram!

## ❓ FAQ

### Q: Нужно ли мне менять токены?
**A:** Нет! Токены остаются такими же.

### Q: Почему теперь только одно окно?
**A:** Бот теперь интегрирован в FastAPI, не нужен отдельный процесс.

### Q: Что такое webhook?
**A:** Это способ получения обновлений от Telegram. Вместо того, чтобы постоянно спрашивать "Есть новые сообщения?", Telegram сам отправляет их на ваш сервер.

### Q: Зачем нужен ngrok?
**A:** Telegram может отправлять webhook только на публичный URL (доступный из интернета). ngrok создает временный публичный URL для вашего локального сервера.

### Q: Нужно ли настраивать webhook каждый раз?
**A:** Если вы используете ngrok - да, потому что URL меняется при каждом запуске. Для продакшена используйте постоянный домен.

### Q: Старый telegram_bot.py ещё работает?
**A:** Да, файл остался, но теперь не используется. Новая архитектура лучше и рекомендуется.

### Q: Что если у меня уже настроен webhook?
**A:** Проверьте командой `python setup_telegram_webhook.py info`. Если URL правильный - всё ОК.

### Q: Бот не отвечает, что делать?
**A:** Проверьте по порядку:
1. Запущен ли сервер? (окно SALESBOT_API)
2. Показывает ли health endpoint `"telegram_bot": "enabled"`?
3. Настроен ли webhook? (`python setup_telegram_webhook.py info`)
4. Работает ли ngrok? (проверьте в окне ngrok)
5. Правильный ли URL в webhook? (должен совпадать с URL от ngrok)

### Q: Можно ли не использовать webhook?
**A:** Да, если очень нужно, можно запустить старый `telegram_bot.py` отдельно. Но webhook - это лучше!

## 📚 Дополнительная документация

- **[TELEGRAM_WEBHOOK_SETUP.md](TELEGRAM_WEBHOOK_SETUP.md)** - Подробное руководство по настройке
- **[АРХИТЕКТУРА_БОТА.md](АРХИТЕКТУРА_БОТА.md)** - Техническое описание архитектуры
- **[README.md](README.md)** - Обновленная главная документация

## 🎉 Преимущества новой архитектуры

1. **Проще** - Один процесс вместо двух
2. **Надёжнее** - Telegram сам отправляет обновления
3. **Быстрее** - Нет задержки на опрос API
4. **Продакшн-ready** - Готово к деплою на сервер
5. **Масштабируемо** - Можно запустить несколько инстансов

## 🔧 Нужна помощь?

Если что-то не работает:

1. Проверьте инструкции выше
2. Посмотрите логи в окне SALESBOT_API
3. Запустите `python validate_setup.py`
4. Создайте issue с описанием проблемы

---

**Теперь ваш бот работает через webhook и готов отвечать вам в Telegram!** 🚀
